import { AuthStorage } from '../types';
export declare class MemoryStore implements AuthStorage {
    private values;
    getItem<T = any>(key: string): Promise<T | undefined>;
    setItem(key: string, value: any): Promise<void>;
}
